node_modules/
.env
.env.*
!.env.example
npm-debug.log*
*.log
.DS_Store

# Research artifacts — generated by scripts, never committed.
# These are large (the BTCUSD 5m dataset alone is ~36 MB) and are
# reproducible from the scripts that made them.
artifacts/
