{
  "name": "tradeify-crypto-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Telegram-controlled Tradeify bot foundation for Railway",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node index.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check index.mjs && node --check src/config.js && node --check src/database.js && node --check src/riskEngine.js && node --check src/tradeifyService.js && node --check src/telegramBot.js"
  },
  "dependencies": {
    "dotenv": "17.4.2",
    "node-telegram-bot-api": "1.2.0",
    "pg": "8.23.0"
  }
}
