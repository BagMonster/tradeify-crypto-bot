# Tradeify Crypto Bot — Implementation Decision Log

---

# ⚠ READ THIS FIRST — Orientation for developers (added 2026-08-19)

**This log is append-only. Historical entries below are preserved verbatim, including ones that are now superseded. Nothing has been deleted or edited.** To find out whether a given entry still governs, use the status index in §0.3 — do not assume an entry is live just because it is written in the present tense.

## 0.1 Where the project actually stands

The project changed direction materially on 2026-08-16 (D-020). If you are joining now, this is the shape of it:

1. **The four planned strategies are dead.** Donchian, TS-momentum, mean reversion and compression breakout were all net negative on a real year of BTC data. D-020 dropped Slots 1–3 and left Slot 4 permanently empty.
2. **One experimental strategy remains: box-fade.** It is a prior-day-high/low mean-reversion strategy. It is not one of the four D-009 slots and has never been part of the controlled build.
3. **Box-fade's only positive result is unvalidated.** +$477.01 over **16 trades**, produced by sandbox code that has since been **deleted**, selected by sweeping parameters against the same four folds it was then judged on. It has never been tested on data it was not shaped by.
4. **The market-data source moved from Binance to DXtrade** (D-020 point 3). Every existing research number must be re-earned on DXtrade candles before it can be cited.
5. **Chapter 26 is superseded, not completed.** Steps 26.7–26.9 are cancelled and the sealed holdout partition is never opened (proposed D-027).

**Nothing in this project has been validated out-of-sample. There is currently no evidence that any strategy works.** Both execution locks remain `false` and no order-placement code exists anywhere in the repository.

## 0.2 The three breaking changes

| # | What broke | Entry | What it means for you |
|---|---|---|---|
| B1 | **Binance is no longer the research data source** | D-020 pt 3 | Do not build a Binance fetch path. D-001's Binance-as-backtest-source provision is superseded **for new work**. Binance-sourced results from Steps 26.2–26.6 remain valid history and are **not** retroactively invalidated. |
| B2 | **The four-slot structure is abandoned** | D-020 pt 1 | D-009's slot architecture, and the Step 26.1 freeze contract's Section 3 slot definitions, no longer describe active work. They remain authoritative for *interpreting* Steps 26.1–26.6 only. |
| B3 | **Chapter 26's remaining steps are cancelled** | proposed D-027 | `docs/chapter-26-slot4-freeze-record.json` is never written. Do not run folds 5–6. **Do not open the holdout partition (15m bars 29,201–35,040).** It is a one-shot resource and the strategies it would validate have been dropped. |

## 0.3 Status index — which entries still govern

| Entry | Status | Note |
|---|---|---|
| D-001 | **PARTLY SUPERSEDED** | Binance-as-backtest-source superseded by D-020 pt 3. The `source`/`symbol` preservation requirement and DXtrade's live-authority role both **still stand**. |
| D-002 | **LIVE, narrowing proposed** | Its Binance-vs-DXtrade cross-feed gate is unsatisfiable under D-020. Proposed D-026 retires *only* that half. The account-day-reset observation, pass/fail thresholds and 7-day canary monitoring **still stand and still block live execution**. |
| D-003 – D-008 | **LIVE** | Unaffected. D-004 (security checkpoint) and D-007 (command-reference completion gate) remain hard gates. |
| D-009 | **SUPERSEDED** | Four-slot structure replaced by D-020 pt 1. |
| D-010 | **LIVE** | Account facts, floors, daily controls and the 0.04% commission assumption all still govern. |
| D-011 | **SUPERSEDED** | Governed a slot that is now permanently empty. |
| D-012 – D-016 | **LIVE** | Regime taxonomy, burn-in, warm-up minimums, overnight-cost model and file-based evidence export all still apply. D-016's PostgreSQL ban is relaxed by proposed D-023. |
| D-017, D-018 | **LIVE** | File-addition records. Proposed D-022 removes the need for future entries of this kind. |
| D-019 | **LIVE but not the forward rule** | The BTC 3.34%–4.21% middle-third band governs *historical* box-fade research. The forward test uses a **rolling percentile** rule instead — see the freeze record §3.6. Do not confuse the two. |
| D-020 | **LIVE — the current direction** | Read this entry in full before doing anything. |
| D-021 – D-027 | **PROPOSED** | Appended in Part 3 below. Not approved. Do not treat as governing. |

## 0.4 Companion documents

This log records *decisions*. It does not record the current strategy definition or the work plan. Those live in the project workspace:

| Document | What it is |
|---|---|
| `box-fade-freeze-record-2026-08-18.md` | **Revision 3, frozen.** The exact strategy configuration, coin-inclusion rules, regime windows and judgment protocol. Pre-registered so none of it can be chosen after results are seen. **If this log and that record disagree about box-fade, the record wins.** |
| `box-fade-parameters-recovered-2026-08-18.md` | Where the box-fade parameters were recovered from, with `[TRANSCRIBED]` / `[MEASURED]` / `[INFERRED]` provenance labels. |
| `session-handoff-2026-08-19.md` | State of the world, known traps, what is verified vs assumed. |
| `build-plan-2026-08-19.md` | What to build, in what order, and where the wasted effort is. |
| `chapter-26-step-26-6-real-data-findings.md` | The full research record behind D-019 and D-020. Long, but it is where every number came from. |

## 0.5 House rules for this file

1. **Append. Never rewrite.** A superseded entry stays, marked superseded. The value of this log is that you can reconstruct what was believed and when.
2. **Check the highest existing ID on `chapter-26-research` before numbering a new entry.** This has gone wrong twice — once producing a D-017/D-019 correction, once producing duplicate D-020 drafts. `main` lags behind and is not a reliable source for the next number.
3. **A decision is not an implementation.** An entry marked *approved* means the direction is settled, not that code exists.
4. **Record the reasoning, not just the conclusion.** Every entry below states what changed, why, what it affects, and how it will be verified. Match that.

---

# Parts 1–2 — Original log, preserved verbatim

This log records approved and proposed changes or additions to the baseline build process in `Tradeify_Familiar_Format_Build_Manual.md`.

An approved entry supersedes the corresponding baseline requirement or adds a governing build gate. An entry marked **approved** is not **implemented** until its affected files, settings, or process step have been completed and its required test has passed.

| ID | Manual section | Original requirement | Proposed or approved replacement/addition | Reason for change | Files or settings affected | Required test or checkpoint | Status |
|---|---|---|---|---|---|---|---|
| D-001 | Market-data design; Chapters 18, 20, and 22 | The original external historical/backfill source assumptions did not establish Binance `BTCUSDT` as the governing source. | Use completed, UTC-aligned Binance `BTCUSDT` candles for historical backfill, strategy backtesting, external charting, initial indicator history, and comparison with DXtrade. Preserve `source` and `symbol` with stored bars. DXtrade remains authoritative for live signals, entry references, stops, targets, execution-time sizing, orders, fills, and position monitoring. Binance must never place or control Tradeify trades. | Binance BTCUSDT is the approved external comparison source, but its USDT price must not be assumed identical to DXtrade synthetic USD pricing. | Market-data ingestion, bar schema, backtester, indicator warm-up, charting instructions, and feed-comparison reports. | Verify completed UTC candle alignment; preserve source and symbol; detect malformed, missing, duplicate, and delayed bars; measure and document Binance–DXtrade OHLC differences. | Implemented through Chapter 22; live DXtrade comparison remains pending |
| D-002 | Chapter 22 — basis validation | Fourteen acceptable comparison days before automation. | Complete at least 24 continuous hours of Binance-versus-DXtrade comparison before restricted canary operation can be considered, observe at least one Tradeify account-day reset, and define measurable pass/fail thresholds. Continue comparison for at least seven days during restricted canary operation. Do not increase risk until extended comparison and live execution results are reviewed. | Provides a staged validation framework without assuming the two feeds are numerically identical or treating an arbitrary duration as the only evidence. | Validation gates, monitoring, readiness reporting, and canary-risk review. | Align timestamps and candle boundaries; compare OHLC; check malformed, missing, duplicate, and delayed bars; observe an account-day reset; apply approved pass/fail thresholds; review seven-day canary comparison before any risk increase. | Approved |
| D-003 | Chapter 22 — backtest history and acceptance | Test at least 12 calendar months of 15-minute data as a hard prerequisite. | Use as much useful history as reasonably available, preferably up to 12 months. Final acceptance thresholds will be chosen after results are visible using qualifying-trade count, no-lookahead construction, realistic next-bar fills, verified fees, modeled slippage, out-of-sample expectancy, drawdown, longest losing streak, Tradeify-floor behavior, parameter stability, and different market conditions. | Calendar duration alone does not establish statistical adequacy or practical robustness. | Backtest runner, reports, validation checklist, and readiness decision. | Present the full backtest and out-of-sample results before thresholds are approved; verify costs, fill timing, drawdown, losing streaks, floor behavior, stability, and market-regime coverage. | Historical ingestion implemented and deployment-verified; backtest acceptance remains pending |
| D-004 | New process gate before Chapter 19 — Account activation discovery | The baseline manual requires careful secret handling but does not require a Codex Security plugin review before DXtrade integration begins. | Install Codex Security and complete a security review immediately before starting Chapter 19 or adding any DXtrade credential, authentication flow, read-only DXtrade client, session handling, or order-routing code—whichever would occur first. Remind the user at that checkpoint. Any actionable high-severity finding blocks DXtrade integration until corrected and rechecked. | Adds an explicit review of secret handling, Telegram authorization, database access, authentication/session management, dependencies, emergency controls, and future execution paths before broker access is introduced. | Plugin setup; repository security review; dependency and secret-handling review; Chapter 19 readiness checklist. No current Stage A code or settings are changed by approving this gate. | Confirm Codex Security is installed; scan the then-current repository; document findings; correct actionable high-severity findings; rerun the review; obtain a passing/accepted checkpoint before Chapter 19 work proceeds. | Implemented; medium hardening tracked |
| D-005 | Chapter 4 — project file map | The baseline repository map contains only application, configuration, test, workflow, and README files; it does not include the required implementation decision log. | Add `docs/implementation-decision-log.md` to the repository and keep it synchronized with every approved, rejected, proposed, or implemented deviation. | Places the governing decisions beside the code they affect, provides GitHub change history, and prevents later work from reverting to superseded requirements. | Repository file map; Chapter 6 file-creation sequence; future commits that change governed requirements. | Confirm the file exists at the exact path, contains D-001 through D-005, and matches the currently governing decisions before application code is created. | Implemented |
| D-006 | Chapter 6.5 — account configuration identity | The baseline `config/account.json` stores account-rule numbers but does not explicitly identify whether they belong to Tradeify Crypto Instant Funding or the separate Tradeify futures Lightning product. | Add `"provider": "tradeify-crypto"` and `"accountType": "instant-funding"` while retaining the verified $50K values, including `"profitSplit": 0.95` for the selected Profit Split 95% add-on. Stop referring to this account as Lightning. | Prevents futures rules from being mixed into the crypto bot and makes later validation, status reporting, and account activation checks unambiguous. | `config/account.json`; later `src/config.js` validation; README/status wording; Chapter 19 account-activation reconciliation. | Validate both identity fields at startup; verify the numeric configuration against the Tradeify Crypto dashboard/contract after purchase; confirm 2:1 leverage unless Double Leverage was purchased; test $48,500 daily floor and $47,000 initial trailing floor; confirm payout-lock behavior. | Implemented and account-verified |
| D-007 | New Chapter 28 — final operator command reference | The baseline ends with a handoff summary but does not require a complete, tested operator reference for the final Telegram command and button set. | Before final handoff, create `docs/telegram-command-reference.md` containing exact syntax, short instructions, argument examples, authorization requirements, expected responses, safety limitations, button mappings, and common-error corrections for every implemented action. Keep Telegram `/help` synchronized and link the guide from README. | Ensures the completed bot can be operated safely without relying on conversation history and prevents implemented commands or later changes from being omitted from the instructions. | Build manual Chapter 28; future `docs/telegram-command-reference.md`; `src/telegramBot.js` and later Telegram modules; README; final handoff checklist. | Inventory code registrations and callbacks; compare code, guide, and `/help` in both directions; test every action with the authorized user; test rejection for unauthorized access; verify argument examples and safety restrictions; do not complete final handoff until all checks pass. | Approved |
| D-008 | Post-automation Addendum A, scheduled after the current roadmap, live automation stability, and D-007 completion | The baseline manual contains no owner-only AI development agent or governed Telegram-to-GitHub engineering workflow. | Add `docs/post-automation-development-agent-addendum.md` as approved future scope. It defines an owner-only OpenAI Responses API development assistant with live GitHub evidence, controlled read-only Railway diagnostics, durable conversation/audit state, proposal-bound approvals, isolated branches and PRs, phased write capability, and protected-change controls. No implementation begins now. A separate companion worker is preferred; current trading command meanings are preserved; production shell/Railway mutation access is forbidden; protected trading/risk/security/schema/deployment changes require a final merge confirmation after diff, CI, security, and staging evidence. | Provides the requested deeper Telegram-based development workflow without expanding the present Stage A scope or placing unrestricted AI tooling inside the live trading process. | New addendum document and README link now; future Telegram routing, companion worker, PostgreSQL development tables, OpenAI/GitHub/Railway integrations, CI/branch protection, security review, and D-007 command-reference synchronization. | Before implementation: finish the trading automation roadmap and D-007; complete a fresh Codex Security review; approve the then-current architecture, permissions, retention, staging, rollback, and merge policy. Roll out read-only analysis first, then proposals, then PR automation without auto-merge. Protected changes retain the stronger final-confirmation gate. | Approved for future scope; not implemented |
| D-009 | Chapters 20–22; D-002/D-003 validation; Stage B activation and canary | The baseline leaves a separate 14-day floor/dashboard observation, blocks risk increases during the seven-day comparison, starts from one strategy hypothesis, and holds the restricted canary at $100 risk for at least two weeks. | Adopt the approved accelerated, evidence-gated automation plan detailed below: a 12-month 8/2/2 backtest; four controlled strategy families; deterministic regime-and-direction routing; governed and uncapped-shadow analysis; 24 continuous hours plus one verified account reset instead of a 14-day pre-launch wait; and a $100 canary reviewed on Days 3, 6, 9, and 12 with promotion or removal no later than Day 12. | Allows automatic trading as soon as the completed build passes objective safety, data, backtest, security, and execution checks, while preserving account-protection guardrails and collecting evidence for controlled optimization. | Future bar, indicator, signal, regime-router, backtest, shadow-mode, analytics, order/reconciliation, PostgreSQL, Telegram reporting, strategy configuration, validation documentation, and activation-checklist work. No Stage A runtime file, Railway setting, database schema, Telegram setting, or execution lock changes at approval time. | Implement and test every detailed D-009 requirement; pass D-004 before DXtrade work; verify the activated account; keep both execution locks false until the separately approved activation deployment; verify GitHub Actions, Railway, PostgreSQL, DXtrade reconciliation, and Telegram behavior at the applicable chapters. | Approved; implementation pending |
| D-010 | Chapters 19–22 — activated-account reconciliation | Pre-purchase rules and older materials contained unresolved differences in drawdown timing, leverage, fees, and automated-access wording. | Treat the activated dashboard values as confirmed account identity; use the stricter working interpretation for unresolved mechanics until read-only API/platform evidence proves otherwise: End-of-Trade trailing MLL on each new closed-trade high capped at $50,000; 22:00 UTC daily reset; 2:1 leverage; 0.04% commission on entry and exit; conservative overnight-cost modeling; 20% consistency; first-payout lock; and live-equity breach checks. Tradeify's published guide confirms privately owned API bots are allowed. Any platform mismatch halts progression and requires reconciliation. | Protects the purchased account while allowing the build to continue without unsupported live-order experiments or an unnecessary multi-question support delay. | Chapter 20 read-only discovery, account/risk configuration, backtests, validation reports, and later execution gates. No execution lock changes at approval. | Verify account values through read-only data; verify BTC/SOL metadata, fees, floors, reset, and API behavior; keep Stage B blocked on missing sizing, stop, or reconciliation facts; test stricter floor/cost assumptions before activation. | Approved; implementation pending |
| D-011 | Chapter 26 — Step 26.1 research freeze contract, Slot 4 | D-009 required "one explainable data-discovered candidate" for the fourth controlled strategy slot. | Define Slot 4 as a predeclared volatility-compression breakout grid with exactly four variants: lookback L ∈ {10, 30} bars × compression percentile N ∈ {20th, 40th} of the prior 480-bar (5-day) Bollinger(20,2) bandwidth. Exactly one variant is selected using development-partition evidence only (walk-forward folds 1–4), then frozen before validation or holdout is touched. If no variant qualifies, Slot 4 stays empty and shadow-only — an acceptable outcome, not a failure to work around. | A 4-variant predeclared grid lowers overfitting risk versus open-ended discovery, but it is a tightening of D-009's original wording, not a literal match, so it needs its own record rather than silent adoption. | `src/research/strategies/compressionBreakout.js`; `src/research/router.js`; freeze record written at Step 26.6. | Freeze record commits the selected variant (or records the slot as empty) with config hashes, git commit, and timestamp before folds 5–6 run; no parameter may change after the freeze record is committed. | Approved 2026-08-15; implementation pending |
| D-012 | Chapter 26 — Step 26.1 research freeze contract, regime handling | The production regime classifier (Chapters 23–24) is mean-reversion-specific and stands down whenever daily ADX(14) > 30, i.e., during trending conditions. | Separate the regime taxonomy (a label describing the market) from the permission gate (which routes may trade in that label). Four labels computed from completed daily bars only: `EXCLUDED_VOL` (daily ATR% < 1.5% or > 3.7%, no trade for any route), `RANGE` (in-band, ADX ≤ 25), `TRANSITIONAL` (in-band, 25 < ADX ≤ 30, no trade for any route — preserves production's ADX_UNCERTAIN stand-down), `TREND` (in-band, ADX > 30, now a labelled, evaluable state instead of a blanket stand-down). Slot 3 (mean reversion) is unaffected — it keeps its own internal ADX stand-down via `evaluateSignal` and therefore only ever produces candidates in `RANGE`. | Without this split, Chapter 26 cannot evaluate trend strategies (Slots 1, 2, 4) at all and would falsely conclude they have no edge on this dataset. Both of production's no-trade conditions survive intact as global guards; nothing about the deployed classifier changes. | `src/research/regime.js` (new). No change to `src/signalEngine.js` or the deployed regime classifier. | A route is the triple (strategy, direction, regime label); at most 16 routes exist, of which Slot 3 can occupy only 2 (RANGE long, RANGE short). Test asserts `EXCLUDED_VOL` and `TRANSITIONAL` block every route and that Slot 3 output is unchanged from `evaluateSignal`. | Approved 2026-08-15; implementation pending |
| D-013 | Chapter 26 — Step 26.1 research freeze contract, warm-up | Production Stage A readiness requires at least 50 × 15m, 40 × 4h, and 25 × 1d completed bars (Chapter 23). | For Chapter 26 research only, require a stricter burn-in before any signal may be generated: 150 × 15m, 40 × 4h (unchanged), and 40 × 1d completed bars. The daily requirement binds (40 days = 3,840 15m bars), leaving roughly 203 days of development trading window. | Wilder ADX(14) is a doubly-smoothed series and is not stable at ~1.8× its period; research that qualifies a route for live money should not lean on an unconverged regime classifier. This is stricter than production and changes nothing about the deployed worker's 50/40/25 readiness contract. | `src/research/manifest.js`, `src/research/backtestEngine.js`. `src/indicators.js` readiness function is left byte-for-byte unchanged. | Test asserts no research signal is generated before the 40-daily-bar burn-in; dedicated test asserts production readiness output is unchanged for a fixed input. | Approved 2026-08-15; implementation pending |
| D-014 | Chapter 26 — Step 26.1 research freeze contract, future live activation | Not previously addressed; no per-strategy live warm-up minimum existed beyond the general 50/40/25 readiness contract. | Record, for a future activation chapter only, the minimum 15m bar history required before each Chapter 26 strategy may take live control: Slot 1 Donchian 50; Slot 2 time-series momentum 150; Slot 3 mean reversion 50 (unchanged); Slot 4 compression breakout 500. | Slot 2's 96-bar momentum lookback and Slot 4's 480-bar percentile window both exceed the general 50-bar 15m minimum. If either route ever qualified for live control, Chapter 23's readiness gate would report indicators warm while the route still lacked the history it needs. Recording this now prevents the gap from being discovered at activation instead of planned for. | None at Chapter 26 — informational only, for a future activation chapter's readiness gate. | Future activation chapter must implement and test a per-route warm-up check using these minimums before that route can be enabled for live control. | Approved 2026-08-15; not implemented, future scope |
| D-015 | Chapter 26 — Step 26.1 research freeze contract, cost model | D-010 modeled the platform's 0.033% overnight cost as a conservative buffer pending read-only confirmation of whether it is charged. | For Chapter 26 research only, do not model overnight cost. The hard-flat rule (no entry fills at or after 21:45 UTC; any open position force-exited at the 21:45 UTC bar open), combined with the 24-bar/6-hour time stop, means no research position is ever held overnight, so the charge cannot occur in this backtest. Reinstate overnight-cost modeling immediately if any future chapter relaxes the hard-flat rule. | Modeling a cost that cannot occur under the frozen execution rules would fabricate research evidence rather than conservatively pad it. D-010's overnight-cost buffer is not overridden for any context where hard-flat is relaxed or removed. | `src/research/backtestEngine.js` cost model only. Does not touch `src/riskEngine.js` or any live cost assumption outside Chapter 26 research. | Test asserts no trade in the research evidence export crosses 21:45 UTC without a forced exit; the cost-sensitivity suite (1.0×, 1.5×, 2.0×) runs without an overnight line item. | Approved 2026-08-15; implementation pending |
| D-016 | Chapter 26 — Step 26.1 research freeze contract, evidence storage | D-005 established `docs/implementation-decision-log.md`; no prior decision addressed where Chapter 26 research evidence (trades, routes, reports) would be stored. | Export all Chapter 26 research evidence to files under `artifacts/` (added to `.gitignore`), not to PostgreSQL. A committed human-readable summary at `docs/chapter-26-research-summary.md` accompanies the PR, including rejected routes and their exact rejection reasons. | `src/database.js` is imported by the live worker at startup; research has no business reshaping the schema the production bot boots against. Files are reproducible, reviewable in the PR diff, and carry zero risk to the deployed service. | `.gitignore` (add `artifacts/`); `docs/chapter-26-research-summary.md` (new). No change to `src/database.js` or the PostgreSQL schema. | `git diff` confirms `src/database.js` is untouched; test/CI confirms no PostgreSQL write occurs from any research module. | Approved 2026-08-15; `.gitignore` updated 2026-08-15 during Step 26.6 (had been missed since approval — caught when exported bar files under `artifacts/` showed as untracked in git); `docs/chapter-26-research-summary.md` still pending, due at Step 26.8/26.9 |
| D-017 | Chapter 26 — Step 26.1 freeze contract §11.2 "Modified files" list | Section 11.2 named only `src/indicators.js`, this decision log, and `.gitignore` as files the freeze contract permitted to change after Step 26.1; `src/research/backtestEngine.js` was expected to stay frozen after its Step 26.3 delivery. | Generalize `src/research/backtestEngine.js` to support a per-strategy dynamic exit hook: a `dynamicExitFns` parameter mapping a strategyId to a per-bar exit-check function, and a `pendingExit` field on the open position, so a dynamic exit condition confirmed at a bar's close fills at the next bar's open — by direct analogy to Section 7's entry-fill rule. Presented to the owner as a two-option fork (generalize `backtestEngine.js` vs. build a second, duplicate execution loop inside `router.js`); the owner selected generalizing `backtestEngine.js`, keeping one execution engine as the single source of truth. | Slot 1 (Donchian channel exit) and Slot 2 (TS-momentum EMA cross exit) both require a dynamic, bar-by-bar exit condition that the Step 26.3 static-bracket engine (fixed stop/target/time-stop set once at entry) cannot evaluate. Slot 3 and Slot 4 use only a static bracket and are unaffected — the new `dynamicExitFns` parameter defaults to `{}` and changes nothing for them. | `src/research/backtestEngine.js`; `src/research/router.js` (new — assembles the `dynamicExitFns` map from `strategies/donchian.js` and `strategies/tsMomentum.js`). | All 17 pre-existing `backtestEngine.js` tests pass unmodified with the new parameter defaulted away, proving the change is behavior-preserving; 4 new tests cover the dynamic-exit fill timing, its interaction with the existing entryBarIndex-floor fix, and its precedence over a same-bar static exit condition. | Approved 2026-08-15 (owner selection during Step 26.5); committed and pushed to `chapter-26-research` at commit `65ae580a973ff53a79df283b7a20f97b727bcc2d` |
| D-018 | Chapter 26 — Step 26.1 freeze contract §11.1 "New files" list and §6.2 freeze record | Section 11.1 listed the research modules through Step 26.5 plus `scripts/run-backtest.mjs`; it did not anticipate a walk-forward fold-boundary/variant-ranking module or a database-bridge export script, and Section 6.2 required a freeze record without specifying its file format or location. | Add `src/research/walkForward.js` (Section 6.1's 6 walk-forward fold boundaries, plus Section 9.4 Slot 4 variant ranking/selection) and `scripts/export-bars-for-research.mjs` (a one-off, owner-run bridge tool using the owner's own `DATABASE_URL` to export completed bars to local JSON files, since Chapter 26's own code may make no network call, DB write, or secret read per Section 12) as new files beyond Section 11.1's original list. Add two purely-additive named exports each to the already-delivered `strategies/donchian.js` and `strategies/tsMomentum.js` (their existing hardcoded entry/exit-period and lookback/EMA-period constants), so the freeze record cites each slot's fixed parameters from a single source of truth instead of a hand-duplicated copy. Write the freeze record itself (`manifest.js`'s new `buildFreezeRecord` export) to a new committed file, `docs/chapter-26-slot4-freeze-record.json`, produced by `scripts/run-backtest.mjs --step 26.6` — never under gitignored `artifacts/`, since Section 6.2 requires the freeze record to be committed. | Folds 1–4's variant selection and the freeze record were both approved requirements (Section 6.1/6.2) with no home in Section 11.1's file list; rather than force this logic into an already-frozen file or silently invent an undocumented file, both are recorded here as scope additions with an explicit safety review: `walkForward.js` performs pure index arithmetic and ranking on already-computed trade summaries — no network/DB/secret access. `export-bars-for-research.mjs` lives outside `src/research/`, is never imported by any research module or by `index.mjs`, and only the owner ever runs it, with their own already-authorized `DATABASE_URL` — the same safety carve-out Section 12 already grants `scripts/run-backtest.mjs` ("a research tool that happens to live in the repository, not a component of the bot") extends identically to this file. Section 9.4's "parameter stability" criterion is undefined numerically; this module reads it as profitable-fold-count then worst-single-fold net P&L, checked only after net profit and profit/drawdown, flagged here as an interpretation rather than a silent choice. | `src/research/walkForward.js` (new); `tests/research.walkForward.test.mjs` (new); `scripts/export-bars-for-research.mjs` (new); `scripts/run-backtest.mjs` (new, per Section 11.1); `src/research/manifest.js` (additive `buildFreezeRecord` export); `tests/research.manifest.test.mjs` (new tests); `src/research/strategies/donchian.js` and `tsMomentum.js` (additive exports only); `docs/chapter-26-slot4-freeze-record.json` (new, produced when the owner runs `run-backtest.mjs --step 26.6`). | All pre-existing tests pass unmodified (235/235 locally); `walkForwardFold`'s boundary math is asserted against Section 6.1's exact fold table (folds 1, 4, and 6 spot-checked against the contract's published bar ranges); `rankSlot4Variants`'s tie-break order is covered by dedicated tests, including ties broken purely on worst-fold net P&L with net profit and profit/drawdown held equal; `buildFreezeRecord` requires exactly one rank-1 variant and a matching `slots.slot4.id`; a full local dry run (synthetic dataset, temporary git repository) confirmed `run-backtest.mjs --step 26.6` runs end-to-end and produces a well-formed freeze record before delivery. | Approved 2026-08-15 (Step 26.6 build); implemented locally, pending commit/push; the freeze record itself is pending the owner running both scripts against the real dataset |
| D-019 | Chapter 26 — box-fade regime band (research, experimental strategy — not one of the four D-009 slots) | The box-fade/PDH-PDL experimental strategy's regime band had been tested under many different values across sandbox research (official BTC 1.5%-3.7%, per-currency ATR%-only 85%-coverage bands, widened-ADX/tighter-target sweeps) with no single value recorded as the strategy's governing rule. | Adopt, for box-fade research going forward, each currency's middle-third sub-band of its own 85%-coverage ATR% band, keeping adxMax=25 fixed: BTC minDailyAtrPct=0.0334 (3.34%), maxDailyAtrPct=0.0421 (4.21%); SOL (research-only; SOL remains disabled for live trading per Section 1/5) minDailyAtrPct=0.0527 (5.27%), maxDailyAtrPct=0.0651 (6.51%). | A dedicated per-trade band-position analysis (`diagnose-atr-band-position-vs-performance.mjs`, sandbox-only) found that trades whose underlying day sits in the middle third of the 85%-coverage band clearly outperform trades near either edge, for both currencies — the only positive-avgR tercile in either case (BTC +0.034R/n=28; SOL +0.256R/n=19), while the high (trend-adjacent) edge was the worst-performing tercile for both (BTC -0.674R/n=10; SOL -0.725R/n=7). | Governs the box-fade/PDH-PDL experimental strategy only. Does NOT change `config/strategy.json`'s regime block, which governs the unrelated Slot 3 Bollinger/RSI mean-reversion strategy and was never tested with this band — box-fade has no dedicated config file yet since it remains uncommitted/sandbox-only and is not part of the D-009/D-011–D-018 Chapter 26 controlled-strategy build. | Before this band governs any live or paper-forward box-fade signal, it must be (a) re-validated on folds 5–6 or later out-of-sample data (this band was derived and selected on the same folds 1–4 used throughout this research), and (b) formally wired into whatever config box-fade eventually reads from, with a follow-up decision-log update recording that wiring. | Approved (research-only) 2026-08-16; box-fade itself remains unimplemented/uncommitted in the repository. Both execution locks (`AUTO_EXECUTE`, `execution.autoExecute`) remain `false`, unaffected by this decision. |
| D-020 | D-009's four controlled strategy slots (Section 3 of the Step 26.1 freeze contract); D-001's Binance-as-backtest-source provision | Step 26.6's real-data run found all four D-009 slots net negative on the full year of BTCUSDT data (Slot 1 Donchian -$7,024.27, 15.3% WR; Slot 2 TS-momentum -$6,842.40, 8.2% WR; Slot 3 mean reversion -$762.51, 38.9% WR; Slot 4 compression breakout net negative in all 4 predeclared variants), while the unofficial, non-D-009 box-fade experimental strategy showed the only positive result found anywhere in Chapter 26 (+$477.01/16 trades, unvalidated out-of-sample). D-001 established Binance BTCUSDT as the historical backtest data source, with DXtrade reserved for live signals/execution only. | (1) Slots 1, 2, and 3 (Donchian, TS-momentum, mean reversion) are dropped from further consideration; Slot 4 remains empty/shadow-only per the freeze contract's existing fallback (Section 6.2, already anticipated, not a new decision). Box-fade becomes the sole strategy under active consideration going forward, superseding D-009's four-slot structure. (2) This is conditioned, not unconditional: before any box-fade variant is treated as more than a promising in-sample finding or considered for any live/paper-forward use, it must be validated out-of-sample. The owner directed that all 9 "leading" variants from the Step 26.6 deep-dive be tested out-of-sample, not just the single leading candidate — noting for the record (per Addendum 9 of the findings document) that these 9 variants are actually only ~2 independent entry rules (confirm-candle and Unsharp) with a handful of overlapping filters layered on, sharing 34 of 38 total underlying trades, so "test all 9" should be read as testing every filter combination against out-of-sample data, not as 9 independent confirmations of an edge. (3) Historical backtesting data source shifts from Binance BTCUSDT to DXtrade's own REST market-data history (`POST /dxsca-web/marketdata`, Candle event type, confirmed to support 15m/4h/1d and finer timeframes down to 1-minute — see `docs/dxtrade-api-endpoint-reference.md`) for the box-fade out-of-sample validation and for future research going forward. This supersedes D-001's Binance-as-backtest-source provision for new research; it does not retroactively invalidate the Binance-sourced Steps 26.2-26.6 results above, and does not change D-001's DXtrade-vs-Binance live comparison role or D-002's unimplemented comparison gate. | The owner judges further investment in three decisively-to-marginally negative strategies unwarranted given the real-data results, and wants the one strategy still under consideration tested against the data source the account actually trades on (DXtrade), rather than Binance's USDT-quoted proxy, before drawing further conclusions. | None yet. This decision does not itself modify `src/research/*`, `config/strategy.json`, `src/dxtradeClient.js`, or any other repository file — it only records the governing direction. Follow-up implementation work (not yet started, each requiring its own step and confirmation per the project's working method): (a) confirming the exact DXtrade instrument symbol/account code for BTC via the existing read-only `DxtradeReadOnlyClient`'s instrument lookup; (b) extending `dxtradeClient.js` with a historical-candle REST method (the client already authenticates and holds read-only account/instrument/quote-subscription methods per its existing, previously-security-reviewed design — this would be an additive read-only method in the same pattern, not new credential/auth work, but still needs its own focused security review before landing, consistent with every prior Chapter 26 step); (c) committing `src/research/strategies/boxFade.js` and its tests to the repository (currently sandbox-only, per Section 18 item 5 of the 4th Project State, now effectively decided by this entry rather than left open); (d) building the out-of-sample validation harness against DXtrade-sourced data. | Section 9.4's existing qualification bar (≥30 trades, net profit across full year/validation/holdout, positive expectancy, no single-trade dependence, parameter stability, zero account failures, ≥1,000 Monte Carlo shuffles with zero failures, qualifies at 1.0x and 1.5x costs) is not lowered or waived by this decision — box-fade must still clear it before any live/paper-forward use. Each follow-up implementation item above requires its own outcome check before merge, per Section 13 of the freeze contract. | Approved 2026-08-16 (owner directive, in chat, following an explicit two-question clarification on scope — confirmed Slots 1/2/3 all drop, and confirmed out-of-sample validation must run before this is treated as final for any live use). Implementation not started. Both execution locks (`AUTO_EXECUTE`, `execution.autoExecute`) remain `false`, unaffected by this decision. |

## D-009 — Accelerated evidence-gated automation plan

D-009 supersedes only the conflicting timing, strategy-research, validation, and canary language identified here. D-001 through D-008 otherwise remain governing. Approval records future implementation requirements; it does not authorize immediate DXtrade work or change the current Stage A deployment.

### Strategy research and backtest design

- Use 12 months of completed, UTC-aligned Binance `BTCUSDT` data as the primary research dataset. Preserve `source` and `symbol`; Binance remains prohibited from placing or controlling Tradeify trades.
- Divide the year chronologically into eight development months, two validation months, and two untouched holdout months. Perform rolling walk-forward checks within the first ten months. Freeze a candidate before it sees the holdout.
- Test four controlled slots: a Donchian-channel breakout/trend strategy; a moving-average or time-series-momentum/trend-pullback strategy; the Bollinger/RSI mean-reversion hypothesis; and one explainable data-discovered candidate.
- A discovered candidate must be formalized from development data before validation and holdout. Do not mine unlimited indicator combinations and select a historical coincidence.
- Model next-bar execution without lookahead, verified DXtrade fees and lot rules, realistic slippage, the minimum hold, both account floors, all daily controls, the one-position limit, and server-side-stop requirements.
- Run two reports: the governed simulation enforcing the live profit lock and all downside controls, and an uncapped-upside shadow analysis that measures opportunity after the profit lock without authorizing additional live trades.
- Collect at least 1,000 shuffled trade-sequence simulations at the initial $100 risk. A live candidate must produce zero simulated account failures in those runs.

### Qualification and regime-direction routing

- The combined routed system must be net profitable after verified costs over the full year, the validation period, and the untouched holdout period; have positive expectancy; avoid dependence on one exceptional trade; remain stable around reasonable nearby parameters; and produce zero simulated account failures, daily-loss-limit breaches, or active-floor violations.
- Each strategy-direction-regime route requires at least 30 historical trades before it may control live orders. A profitable route with fewer observations remains shadow-only.
- Evaluate long and short performance separately. A strategy that is profitable long but unprofitable short may trade only long; its short side is discarded. A strategy that is profitable short but unprofitable long may trade only short; its long side is discarded.
- Use a deterministic, pretested market-regime decision tree. At most one qualified strategy may control a trade at a time. Other candidates continue in shadow mode.
- Rank passing routes primarily by governed net profit after costs, then profit relative to drawdown, stability, and holdout behavior. Use uncapped shadow results only to identify future upside proposals.
- Permit only the approved winner for the detected regime and direction. Uncertain, extreme, stale, malformed, or conflicting conditions produce no trade. Never switch strategies while a position is open.
- Backtest the complete routing tree as one combined system. Preserve the one-open-position maximum across all strategies and instruments.

### Accelerated pre-launch validation

- Remove the separate requirement for 14 consecutive pre-launch days of bot-floor/dashboard matching.
- Before restricted automatic trading, require at least 24 continuous hours of Binance-versus-DXtrade comparison and one observed 22:00 UTC Tradeify account-day reset.
- Verify that balance, equity, daily floor, MLL floor, and active floor match the activated DXtrade dashboard immediately before and after the reset.
- Verify completed UTC boundaries and measurable pass/fail thresholds for missing, duplicate, malformed, delayed, stale, misaligned, and materially divergent bars.
- Run the comparison and reset observation concurrently with the remaining build so they add no post-build delay when already complete.
- Continue Binance/DXtrade comparison for at least seven days during the canary. A comparison failure pauses automation, but clean continuing comparison does not block an otherwise qualified Day-3 promotion from $100 to $200 risk.

### Restricted canary and 12-day hard cap

- Begin approved automatic execution at a maximum of $100 risk per trade only after the full build, D-004 security, account discovery, D-009 backtest/data requirements, execution/reconciliation tests, and an explicit activation approval pass.
- Review the canary on Tradeify account Days 3, 6, 9, and 12.
- On Days 3, 6, or 9, promote to the configured $200 Stage 1 cap when cumulative live P&L is positive, at least three trades have closed, and every signal, entry, fill, fee, server-side stop, exit, database record, and reconciliation check is correct.
- Before Day 12, positive results with fewer than three trades remain at $100 until the next review or the third completed trade.
- On Day 12, the canary must end in promotion or removal. A positive, operationally clean strategy may promote to $200 with the three-trade minimum waived only when at least one complete live trade verified the entire execution lifecycle.
- At Day 12, a zero- or negative-P&L live strategy is removed rather than promoted; the next qualified shadow candidate may be evaluated under a separately tracked canary. No-trade results end that strategy's canary for review and do not justify a risk increase.
- Any stop rejection, duplicate order, reconciliation mismatch, unexplained position, safety halt, daily hard stop, or other critical execution defect pauses automation and prohibits promotion until corrected and rechecked.
- The configured $300 Stage 2 cap still requires the approved $53,000 balance threshold and any applicable later review.

### Persistent evidence and controlled optimization

- Persist source-tagged bars; regime inputs and classifications; every live and shadow signal; selected and rejected strategy reasons; full risk-gate snapshots; requested and actual entries; unique client order codes; fills, fees, slippage, stops, targets, exits, maximum favorable/adverse excursion, dollar and R-multiple results, holding time, daily account values, lock events, configuration version, and Git commit.
- Produce daily summaries by strategy, direction, and regime, including governed results and uncapped-upside shadow opportunity.
- Live evidence may generate an optimization proposal, but the bot must not silently rewrite or activate its own rules. Every material optimization requires an exact proposal, backtest against untouched data, comparison with the deployed version, owner approval, decision-log update, tests, and verified deployment.
- All existing account safeguards remain binding unless a later approved decision explicitly changes them: daily loss controls, profit lock, active-floor margin, one open position, durable pause, safety halt, shared risk gate, idempotency, reconciliation, confirmed server-side stops, audit logging, and both execution locks.

## D-010 — Activated-account rule reconciliation

### Confirmed account facts

- Tradeify Crypto Instant Funding account; active DXtrade access.
- Starting balance: $50,000.
- Profit Split 95% add-on.
- Maximum daily loss: $1,500.
- Maximum drawdown amount: $3,000.
- KYC, 2FA, and contract signing are complete.
- Tradeify's published DXtrade Platform Guide expressly allows API automation by a bot owned by the trader and confirms Market, Limit, Stop, and Stop-Limit orders plus pre-entry SL/TP support.

### Conservative working rules for unresolved mechanics

- Model the maximum-loss floor as End-of-Trade trailing: it ratchets after each newly profitable closed-trade high, never moves down, and caps at $50,000.
- Use 22:00 UTC as the Tradeify account-day reset.
- Enforce no more than 2:1 leverage on all pairs.
- Model 0.04% commission on entry and 0.04% on exit.
- Backtests may include the contract's 0.033% overnight cost as a conservative buffer until read-only account evidence confirms whether it is charged.
- Apply the 20% consistency requirement and first-payout drawdown lock.
- Use live equity, including unrealized P&L, for breach protection.
- Treat the platform/account metrics as the system of record. Any mismatch pauses progression and requires reconciliation.
- Binance remains data-only and may never place, route, or control Tradeify orders.

These rules may be made stricter without a new approval. Relaxing them, enabling execution, or replacing an unresolved assumption requires evidence, an approved decision-log update, tests, and a verified deployment.

## Step 26.1 — Chapter 26 research freeze contract (D-011–D-016)

`claude/chapter-26-step-26-1-freeze-contract.md` amended and superseded the Step 26.1 draft produced in a prior chat, after that draft was reviewed and found not approvable as written: an undefined UTC-calendar partition scheme on a 365-day dataset that does not start on the first of a month; three new indicator functions (EMA, Donchian, momentum, bandwidth percentile) required by Slots 1, 2, and 4 but not mentioned; Slot 2's 96-bar lookback exceeding the production 15m warm-up minimum; an overclaimed "verified" Railway status that was in fact owner-reported; and several underspecified items (timeframes, overnight cost vs. hard-flat, the $100 cap's exact meaning, D-010 MLL mechanics, evidence destination, file and test names). A further conflict was found while amending: the production regime classifier stands down whenever ADX > 30, which would block Donchian and time-series-momentum evaluation almost entirely. This is resolved by D-012 below.

Six open decisions (A–F) were presented for approval and were **approved as written, without amendment, on 2026-08-15**:

- **A** — Regime taxonomy separated from the mean-reversion permission gate. Recorded as **D-012**.
- **B** — Research burn-in of 40 completed daily bars (production's 25 unchanged). Recorded as **D-013**.
- **C** — Rolling walk-forward, 4-twelfths train / 1-twelfth test, 6 folds over the first 10 twelfths. This matches D-009's original wording ("rolling… within the first ten months") literally rather than deviating from it, so it does not receive its own D-0xx entry — it corrects the prior draft's anchored/expanding-window error back to the already-approved D-009 methodology.
- **D** — Evidence exported to files, not written to PostgreSQL. Recorded as **D-016**.
- **E** — Overnight cost not modeled while hard-flat at 21:45 UTC is enforced. Recorded as **D-015**.
- **F** — Slot 4 as a 4-variant predeclared compression-breakout grid. Recorded as **D-011**.

**D-014** (per-strategy live warm-up minimums) was derived from the same contract's Section 4.2 as a consequence of B and the new indicators, and is recorded for a future activation chapter only — it changes nothing at Chapter 26.

Partitions were approved as **fixed bar count**, not calendar boundaries: development bars 1–23,360 (8 twelfths), validation 23,361–29,200 (2 twelfths), holdout 29,201–35,040 (2 twelfths, sealed), at 2,920 bars per twelfth. 4h and 1d bars are assigned to partitions by close time against the two derived boundary timestamps, never by their own bar counts, because 2,190 ÷ 12 does not divide evenly.

Approval of this contract freezes it: no Step from 26.2 onward may quietly change a definition made here. No repository file has been changed by this approval; both execution locks remain false. Step 26.2 (feature branch, then `manifest.js`, `regime.js`, `accountModel.js` and their tests) begins only on separate owner confirmation.

## Step 26.2–26.5 — Chapter 26 controlled-strategy research build

- **Step 26.2** (`src/research/manifest.js`, `regime.js`, `accountModel.js` + tests): completed and pushed to `chapter-26-research` at commit `09e5e9816623c1ed4525f34e5f20175139da6ced`. Implements Section 2's dataset verification and fixed-bar-count partitions, D-012's regime taxonomy, and Section 9's research account-state model wrapping `src/riskEngine.js` unmodified. Confirmed present on both the local branch and `origin/chapter-26-research` refs via git-ref inspection; GitHub Actions and Railway status for this commit are owner-reported, not independently checked in this session.
- **Step 26.3** (`src/research/strategies/meanReversion.js`, `backtestEngine.js` + tests): completed and pushed at commit `832991d98cf22849683711330a78921add802bff`. `meanReversion.js` wraps `src/signalEngine.js`'s `evaluateSignal` unmodified — Slot 3 can never quietly diverge from the deployed strategy. `backtestEngine.js` is the bar-by-bar backtest loop: next-bar-open fills, protective-stop/target/time-stop/hard-flat exit precedence, gap-aware stop fills, slippage and commission costing, one-position enforcement, account-day boundary resets, and account-failure force-flatten. 125/125 tests passing locally (70 pre-existing + 32 from Step 26.2 + 23 new). Three interpretive calls were flagged: target fills at the exact target price, never a favorable gap price; time-stop exits fill at that bar's open, by analogy to hard-flat; an account failure force-flattens the open position and halts the run. One bug was caught and fixed pre-delivery: a position entered on the very last permitted decision bar could be force-closed on a bar before its own entry; fixed so the close-out bar is always at or after the entry bar, pinned by a dedicated test. Confirmed present on both branch refs; Actions/Railway owner-reported.
- **Step 26.4** (four additive `src/indicators.js` exports, plus `strategies/donchian.js`, `tsMomentum.js`, `compressionBreakout.js` + tests): completed and pushed at commit `24df65a271363f774025c3c6be8e88be68949f7c`. Added `ema`, `donchianChannel`, `timeSeriesMomentum`, `bollingerBandwidthPercentile` to `src/indicators.js` (diff-confirmed additions-only). Slot 1 (Donchian) and Slot 2 (TS momentum) entry logic, plus their standalone, tested dynamic-exit-check functions (not yet wired into the engine at this step — see D-017); Slot 4 (compression breakout) entry logic across its 4-variant grid, fully compatible with the Step 26.3 static-bracket engine since it has no dynamic exit. 161/161 tests passing locally (125 pre-existing + 36 new). Flagged: the dynamic-exit wiring gap between Slots 1/2 and `backtestEngine.js`, resolved in Step 26.5 (D-017); compression breakout's percentile method (nearest-rank) was an implementation choice, since the contract names "the Nth percentile" without specifying a method; each module's own warm-up floor is its standalone minimum-computability requirement, not the Section 4.2 future-activation table (D-014). Confirmed present on both branch refs; Actions/Railway owner-reported.
- **Step 26.5** (`backtestEngine.js` generalized per D-017; `router.js`, `metrics.js`, `monteCarlo.js` + tests, new): committed and pushed to `chapter-26-research` at commit `65ae580a973ff53a79df283b7a20f97b727bcc2d`, confirmed via both branch refs. `router.js` assembles all four slots into one `signalFn` plus a `dynamicExitFns` map, gates every candidate on D-012's regime taxonomy before any strategy is evaluated, and uses a documented, overridable, provisional tie-break priority (Slot 1 → 2 → 3 → 4) pending a real qualification ranking from Steps 26.6/26.7 — flagged to the owner as a placeholder, not a claim about relative strategy quality. `metrics.js` computes per-trade-list win rate, expectancy, realized-P&L-curve max drawdown, win/loss streaks, MAE/MFE, and route/partition breakdowns, reusing `regime.js`'s `routeId` and `manifest.js`'s `partitionForCloseTime` rather than re-implementing them. `monteCarlo.js` implements Section 9.5's seeded `mulberry32` PRNG and Fisher-Yates shuffle, replaying each shuffled trade sequence against the account's MLL floor only — the calendar-bound daily floor is not modeled under a shuffled, non-chronological ordering, flagged to the owner as a documented scope boundary rather than a gap, since the daily floor's real behavior is already fully exercised by the ordinary real-calendar backtest. 213/213 tests passing locally (161 pre-existing + 4 new `backtestEngine.js` tests + 20 `router.js` + 13 `metrics.js` + 15 `monteCarlo.js`). Both execution locks remain false throughout.
- **Step 26.6** (`src/research/walkForward.js` new, per D-018; `manifest.js`'s `buildFreezeRecord` addition; `scripts/export-bars-for-research.mjs` and `scripts/run-backtest.mjs` new + tests): delivered to the owner and staged to the local clone on 2026-08-15; not yet committed/pushed. `walkForward.js` computes Section 6.1's 6 walk-forward fold boundaries (folds 1–4 for Slot 4 selection, folds 5–6 for post-freeze stability reporting only) and ranks the 4 Slot 4 compression-breakout variants per Section 9.4 (net profit → profit/drawdown → an interpreted "parameter stability" tiebreak — see D-018). `manifest.js` gains `buildFreezeRecord`, assembling all four slots' fixed parameters, the full Slot 4 variant comparison, config hashes, git commit, and timestamp into one frozen record. `scripts/export-bars-for-research.mjs` (new, outside `src/research/`, flagged in D-018) is a one-off bridge tool the owner runs with their own `DATABASE_URL` to export completed bars from Railway Postgres to local JSON files — Chapter 26's own code is barred from network/DB/secret access per Section 12, so this is the only piece of tooling permitted to touch the database directly. `scripts/run-backtest.mjs` (new, per Section 11.1) reads those exported files, verifies the dataset, builds the regime timeline, runs each Slot 4 variant across folds 1–4 in isolation, ranks them, and writes the freeze record to `docs/chapter-26-slot4-freeze-record.json`. 235/235 tests passing locally (213 pre-existing + 17 `walkForward.js` + 5 new `manifest.js` tests for `buildFreezeRecord`). A full local dry run against a synthetic dataset confirmed the `run-backtest.mjs --step 26.6` pipeline runs end-to-end and produces a well-formed freeze record. Both execution locks remain false throughout. Two manual steps remain, which only the owner can run (their own `DATABASE_URL` is never exposed to this session): `node scripts/export-bars-for-research.mjs`, then `node scripts/run-backtest.mjs --step 26.6`, with the resulting console output and freeze record reported back before it is committed.

## Current build progress

- Chapter 1 boundary checkpoint: completed on 2026-08-12.
- Chapter 2 architecture checkpoint: completed on 2026-08-12.
- Chapter 3 safety-improvements checkpoint: completed on 2026-08-12.
- Chapter 4 project-file-map checkpoint: completed on 2026-08-12; D-005 approved.
- Chapter 5 GitHub-repository creation: completed and verified on 2026-08-12. Repository: `BagMonster/tradeify-crypto-bot`; visibility: private; default branch: `main`; initial contents: `README.md` only; plugin access: admin/push confirmed.
- Chapter 6 preliminary step required by D-005: `docs/implementation-decision-log.md` created and verified on 2026-08-12.
- Chapter 6.1 `package.json`: completed and verified on 2026-08-12. Commit: `dbb4354`.
- Chapter 6.2 `Procfile`: completed and verified on 2026-08-12. Commit: `eae7169`.
- Chapter 6.3 `.gitignore`: completed and verified on 2026-08-12. Commit: `bfebfe9`.
- Chapter 6.4 `.env.example`: completed and verified on 2026-08-12. Commit: `3d44020`.
- Chapter 6.5 `config/account.json`: completed and verified on 2026-08-12. D-006 approved; configuration commit: `18721ad`.
- Chapter 6.6 `config/strategy.json`: completed and verified on 2026-08-12. Baseline approved as written; sequential trades within an account day are allowed when every gate passes, but only one position may be open at a time. Configuration commit: `ad66f32`.
- Chapter 6.7 `src/config.js`: completed and verified on 2026-08-12. D-006 identity validation implemented for `tradeify-crypto` / `instant-funding`; valid Stage A configuration accepted; unsafe automatic execution and incorrect account identity rejected. Commit: `560db57`.
- Chapter 6.8 `src/riskEngine.js`: completed and verified on 2026-08-12. Focused checks passed for daily and trailing floors, floor ratcheting/cap, payout lock, stages, consistency, daily controls, position sizing, and the shared risk gate. Commit: `e82368a`.
- Chapter 6.9 `src/database.js`: completed and verified on 2026-08-12. Syntax and mocked PostgreSQL checks passed for pool settings, schema initialization, initial account state, numeric normalization, durable pause/resume data, daily ledger reads, event writes, health ping, and shutdown. Commit: `c3c7abc`.
- Chapter 6.10 `src/tradeifyService.js`: completed and verified on 2026-08-12. Focused checks passed for status calculations, persistent pause, hashed two-step resume, incorrect/expired-code rejection, safety-halt preservation, Stage A flat instructions, and health reporting; no order-placement path exists. Commit: `60cfe0e`.
- Chapter 6.11 `src/telegramBot.js`: completed and verified on 2026-08-12. Focused checks passed for `/whoami` discovery, one-user authorization, all Stage A slash commands, all six inline buttons, callback acknowledgement, command/button parity, unknown-button handling, and the registered Telegram command menu. Commit: `7a86783`.
- Chapter 6.12 `index.mjs`: completed and verified on 2026-08-12. Syntax and lifecycle checks passed for configuration → PostgreSQL initialization → control service → Telegram startup, Stage A simulation/auto-execution-off reporting, and clean `SIGTERM`/`SIGINT` shutdown of Telegram polling and PostgreSQL. Commit: `0de139a`.
- Chapter 6.13 `tests/riskEngine.test.mjs`: completed and verified on 2026-08-12. All 13 automated checks passed for initial/daily/trailing floors, MLL ratcheting and cap, payout locking, 20% consistency, required-profit reporting, BTC position sizing, maximum-notional refusal, healthy and blocked risk gates, and two-loss risk reduction. Commit: `4483e69`.
- Chapter 6.14 `.github/workflows/test.yml`: completed and verified on 2026-08-12. YAML validation passed, and GitHub Actions push run #1 completed successfully under Node.js 22 with dependency installation, full JavaScript syntax checks, and all 13 risk tests. Commit: `76b3f82`.
- Chapter 6.15 `README.md`: completed and verified on 2026-08-12. The Stage A feature summary, dual automatic-execution locks, Railway start command, eight operator commands, and simulation-only boundaries match the repository; the Chapter 4 file tree is complete; no embedded token, database credential, API key, password, or secret was found; GitHub Actions run #3 passed. Commit: `6dcb8a2`.
- Chapter 7 GitHub Actions verification: completed on 2026-08-12. The newest completed `Test Tradeify Bot` workflow, run #4, finished successfully; the `test` job and every workflow step were green, and the cloud log reported 13 tests, 13 passes, and 0 failures.
- Chapter 8 Telegram bot creation: completed and user-confirmed on 2026-08-12. The bot was created through the verified `@BotFather` account; its token was stored securely and not shared; group joining was disabled. No repository code, Railway deployment, DXtrade connection, or execution capability was added.
- Chapter 9 Railway topology: completed and user-verified on 2026-08-13. Railway project `Tradeify Crypto Bot` contains one online `Postgres` service with its attached volume and one staged, undeployed `tradeify-crypto-bot` worker sourced from private repository `BagMonster/tradeify-crypto-bot` on `main`. No Redis, cron, public web service/domain, or second worker was added. The worker remains undeployed pending Chapter 10 variables and Chapter 11 deployment checks.
- Chapter 10 Railway variables: completed and user-confirmed on 2026-08-13. Exactly seven worker variables were added: the BotFather token is masked and was not shared; `DATABASE_URL` is a Railway reference to `Postgres.DATABASE_URL`; `TELEGRAM_ALLOWED_USER_ID=0`; `DATABASE_SSL=false`; `APP_MODE=stage-a`; `AUTO_EXECUTE=false`; and `NODE_ENV=production`. Both automatic-execution locks remain false.
- Chapter 11 Railway deployment: completed and user-verified on 2026-08-13. The `tradeify-crypto-bot` worker and `Postgres` are both online; the worker started with `npm start` → `node index.mjs`; Railway reported the deployment active; and logs contained `Tradeify worker started in Stage A simulation mode.` plus `Auto-execution is locked OFF.` No startup error or exposed secret was visible. The npm production-config warning is informational. `TELEGRAM_ALLOWED_USER_ID` remains `0` pending Chapter 12 identity discovery.
- Chapter 12 Telegram authorization: completed and user-confirmed on 2026-08-13. The owner obtained the immutable numeric Telegram ID through `/whoami`, stored it directly in Railway as `TELEGRAM_ALLOWED_USER_ID` without sharing it in chat, and allowed Railway to redeploy. The authorized account then received the complete `/help` command list and all six inline control buttons. Stage A remains simulation-only and both automatic-execution locks remain false.
- Chapter 13 Stage A control-loop verification: completed and user-confirmed on 2026-08-13. `/health` confirmed the Telegram → Railway worker → PostgreSQL path with automatic execution off. `/status` verified Stage A simulation, the approved $50,000 initial account and risk values, daily control `ALLOW`, cold indicators, a stale feed, a disallowed regime, and a blocked entry gate. `/kill` persisted `Bot: PAUSED` through a Railway worker redeploy and changed the entry block to account lockout. The hashed two-step `/resume` → `/confirmresume CODE` flow restored `Bot: RUNNING` without bypassing the cold/stale entry gate. `/flat` returned manual Stage A instructions and placed no order. Both automatic-execution locks remain false; no DXtrade connection or order-placement path exists.
- Chapter 14 code-flow review: completed and user-confirmed on 2026-08-13. The deployed startup, `/status`, inline-button, and Railway-restart paths were reviewed against the baseline manual and current repository. The owner confirmed understanding that `package.json` starts `index.mjs`; configuration is validated before PostgreSQL, the shared service, and Telegram polling start; `/status` uses authorized Telegram routing, durable PostgreSQL state, and shared risk calculations; buttons reuse the same service functions as slash commands; and PostgreSQL preserves account and pause state across worker restarts. No code, database schema, Railway variable, risk rule, or execution setting changed.
- D-009 accelerated evidence-gated automation plan: approved and recorded on 2026-08-13. It governs future strategy research, regime-direction routing, 12-month 8/2/2 backtesting, accelerated 24-hour/reset validation, shadow analytics, and the $100 canary with a Day-12 promote-or-remove cap. Approval is documentation-only at this checkpoint; Stage A runtime and both execution locks remain unchanged.
- Chapter 15 PostgreSQL-table explanation checkpoint: completed and user-confirmed on 2026-08-13. The owner confirmed understanding of `bot_state` as the single current account/control row, `daily_ledger` as UTC daily realized-P&L history, and `events` as the timestamped audit trail; the schema remains created automatically by `src/database.js`, and no SQL or database mutation was required.
- Chapter 28 final command reference: D-007 approved and added to the build manual on 2026-08-12; completion remains pending until the bot's final Telegram command and button set is implemented and verified.
- Post-Automation Addendum A: D-008 approved for future scope on 2026-08-13 after the attached 73-page proposal was reviewed in full and checked against the live repository and current official OpenAI documentation. The governed addendum and README link are documentation-only. Implementation is deferred until the trading automation roadmap and D-007 are complete, stability is demonstrated, and a fresh security/architecture checkpoint passes.
- Chapter 16 risk-protection explanation checkpoint: completed and user-confirmed on 2026-08-13. The owner confirmed understanding of the independent daily and MLL floors, higher active floor, $750 floor safety margin, combined realized/unrealized daily controls, two-loss risk reduction, ATR/floor/daily/stage-constrained sizing, and the shared no-bypass entry gate. D-009 remains future implementation; Stage A runtime and both execution locks remain unchanged.
- Chapter 17 GitHub-update-routine checkpoint: completed and user-confirmed on 2026-08-13. The owner confirmed the standard GitHub edit/commit → green Actions → matching Railway deployment → startup-log review → `/health` → `/status` validation sequence and the rule that any failed GitHub test is a stop sign. No code, Railway setting, Telegram setting, database schema, risk rule, or execution lock changed.
- Chapter 18 intentional-absence checkpoint: completed and user-confirmed on 2026-08-13. Live repository verification confirmed that Stage A contains Telegram control, PostgreSQL persistence, configuration validation, and the shared risk engine but no DXtrade client, authentication/session handler, market-data/bar pipeline, indicators, D-009 strategy router, backtester, order router, or live reconciliation capability. The owner provided the official DXtrade Developer Portal for later Chapter 19 discovery. No API connection, credential use, code, database schema, Railway setting, Telegram setting, risk rule, or execution lock changed.
- D-004 pre-DXtrade security checkpoint: completed on 2026-08-13. Codex Security reviewed all 17 repository files at commit `3554c47` and reported one high-severity fail-open numeric-input finding plus two medium hardening findings. The high finding was fixed in `src/riskEngine.js` and `src/database.js`; five regression tests were added; all 18 tests passed locally and in GitHub Actions run #19; and the exact remediation commit `05651cf` passed a full recheck with zero critical/high findings and two medium findings. The medium items remain governed: commit an npm lockfile and use `npm ci` before adding the first new dependency, and never enable `DATABASE_SSL=true` or use an external PostgreSQL path until trusted certificate verification is implemented and tested. Stage A remains simulation-only, both execution locks remain false, and no DXtrade capability was added.
- Chapter 19 account-activation discovery: completed and user-confirmed on 2026-08-14. The owner completed KYC and 2FA, signed the funded-trader contract, verified that DXtrade opens with trading enabled, and confirmed the active Instant Funding account's $50,000 starting balance, 95% profit split, $1,500 daily loss limit, and $3,000 maximum drawdown. Tradeify's published platform guide confirms privately owned API automation and the supported manual order types. BTC/SOL minimum quantities, quantity increments, exact production API paths, authentication/session behavior, exact stop/bracket fields, idempotency, rejection codes, historical bars, volume semantics, and startup reconciliation remain documented technical discovery items. Stage B remains blocked on any missing sizing, stop, or reconciliation fact. No secret, live order, code, configuration, Railway variable, database schema, or execution lock changed.
- D-010 activated-account rule reconciliation: approved and recorded on 2026-08-14. Confirmed dashboard values govern account identity; unresolved mechanics use the stricter working interpretation until read-only evidence proves otherwise. No execution lock changed.
- Chapter 20 read-only DXtrade discovery client: completed and user-verified on 2026-08-14. `src/dxtradeClient.js` now provides login, ping/keepalive, logout, account/user/portfolio/position/open-order/metrics/instrument reads, and live quote subscriptions while exposing no order-placement, modification, cancellation, position-close, flatten, arbitrary-request, or raw-send methods. Seven focused tests passed locally; a Codex Security diff review reported zero reportable findings after a 401 token-redaction hardening correction; PR #2 was squash-merged as commit `968335c`; GitHub Actions runs #22 and #23 passed; and the resulting Railway deployment was verified through `/health` and `/status`. Stage A and both automatic-execution locks remain false. No DXtrade credential entered GitHub or chat, and no order was placed.
- Chapter 21 PostgreSQL bar storage: completed and user-confirmed on 2026-08-14. PR #3 was squash-merged as commit `5d9a620`; GitHub Actions run #26 passed; the Railway deployment was verified; and the owner then confirmed Chapter 21 complete. The deployed implementation creates the `bars` table automatically and stores only completed UTC-aligned `15m`, `4h`, and `1d` bars with preserved source/symbol identity, strict OHLC/volume validation, idempotent parameterized upserts, atomic batch writes, chronological reads, and warm-up counts. No manual SQL, signal generation, or execution capability was added.
- Chapter 22 Binance historical backfill: completed and user-confirmed on 2026-08-14. PR #4 was squash-merged as commit `7431474` after GitHub Actions run #27 passed; documentation follow-up PR #5 was squash-merged as commit `57d3034` after run #29 passed. Railway then reported every `15m`, `4h`, and `1d` Binance range already complete, exact `BTCUSDT` historical coverage ready, the Tradeify worker running in Stage A, and automatic execution locked off. `src/binanceBackfill.js` uses only unauthenticated GET requests to Binance's public market-data-only host, rejects malformed, misaligned, incomplete, missing, or duplicate bars, bulk-upserts idempotently, and verifies exact range coverage. Binance remains unable to place or control Tradeify trades.
- Chapter 23 deterministic indicators: completed and user-confirmed on 2026-08-14. PR #6 was squash-merged as commit `6fb36e6` after GitHub Actions run #31 passed; documentation checkpoint PR #7 was squash-merged as commit `c47d380` after run #33 passed. Railway verified all three historical ranges, exact Binance `BTCUSDT` coverage, successful indicator warm-up, Stage A simulation, and automatic execution locked off. Telegram `/status` then verified `Indicators warm: YES`, `Feed stale: YES`, `Regime allowed: NO`, and `Entry gate: BLOCKED - Market data feed is stale`. `src/indicators.js` calculates 15-minute Bollinger Bands with population standard deviation, 15-minute RSI and ATR with Wilder smoothing, 4-hour ADX with Wilder smoothing, and daily ATR with Wilder smoothing. It accepts only completed, UTC-aligned, chronological, contiguous, single-source/symbol bars; enforces at least 50 `15m`, 40 `4h`, and 25 `1d` bars; and persists warm only after every calculation succeeds. Fifty-one local tests pass, and a focused Codex Security diff review completed with zero reportable findings. No signal, tradeable-regime, fresh-feed, routing, or execution capability was added.
- Chapter 24 deterministic signal engine: completed and user-confirmed on 2026-08-14. PR #9 was squash-merged as commit `a3868d1` after GitHub Actions run #37 passed; Railway reported the exact deployment successful; and the owner confirmed the deployed checkpoint. `src/signalEngine.js` evaluates the configured Bollinger/RSI mean-reversion candidate only from two consecutive warm indicator snapshots. It classifies the current daily-ATR, ADX, and range-band regime; returns explicit no-signal reasons for cold, malformed, mismatched, non-consecutive, time-regressing, uncertain, extreme, flat, or non-qualifying inputs; produces symmetric long and short candidates; and calculates ATR-based stop, middle-band target, reward/risk, and time-stop references without sizing or routing an order. Snapshot warm-up lineage is bound to Chapter 23's authoritative 50/40/25 requirements, history counts cannot regress, and UTC alignment, higher-timeframe causality, source/symbol identity, and positive trade geometry are validated fail-closed. The security review identified and corrected the self-reported warm-up-minimum weakness before publication, then completed with zero reportable findings. Sixty-two tests pass. The module is not called by the Stage A worker, does not update database permission, make the feed fresh, authorize regime permission, send alerts, or expose execution. Both execution locks remain false.
- Chapter 25 Telegram signal alerts and evidence records: completed and user-confirmed on 2026-08-14. PR #10 was squash-merged as commit `52d1bd8`; GitHub Actions run #39 passed with 70 tests and 0 failures; Railway reported the exact merged commit deployed successfully; and the owner confirmed Chapter 25 complete. `src/signalAlerts.js` validates and formats candidate or no-signal results, lists every blocking gate, states that no order was placed, binds delivery to one positive safe-integer owner chat ID, and persists field-whitelisted prepared/sent/failure evidence without the owner ID, arbitrary input fields, or raw Telegram transport errors. Unsafe control and bidirectional display characters are rejected. The review corrected owner-destination binding and display/error hardening before publication and then completed with zero reportable findings. The module is not called by the current worker, so it sends no production alert, does not make the feed fresh, does not calculate executable size from unverified DXtrade lot rules, does not route an order, and does not enable execution. Both execution locks remain false.
- Chapter 26 Step 26.1 research freeze contract: approved 2026-08-15. Decisions A–F approved as written; recorded as D-011 through D-016 (decision C confirms D-009's original wording and needs no new entry). No repository file other than this decision log has changed, and both execution locks remain false. Step 26.2 (feature branch, `manifest.js`, `regime.js`, `accountModel.js` + tests) is the exact next step and requires separate owner confirmation to begin. Chapter 27 not started.
- Chapter 26 Steps 26.2–26.6 — controlled-strategy research build: see the "Step 26.2–26.5" section above for full detail. Step 26.2 (manifest/regime/accountModel), Step 26.3 (mean-reversion adapter + backtest engine), Step 26.4 (new indicators + Donchian/TS-momentum/compression-breakout strategies), and Step 26.5 (`backtestEngine.js` generalized per D-017, plus new `router.js`, `metrics.js`, `monteCarlo.js`) are committed and pushed to `chapter-26-research` (commits `09e5e98`, `832991d`, `24df65a`, `65ae580`), each confirmed via git-ref inspection. Step 26.6 (`walkForward.js` new per D-018, `manifest.js`'s `buildFreezeRecord`, `scripts/export-bars-for-research.mjs` and `scripts/run-backtest.mjs` new) is delivered and staged to the local clone, pending owner commit/push and then two owner-run manual steps (the bars export and the fold/selection run) before the freeze record itself can be committed. 235/235 tests passing locally as of Step 26.6. Both execution locks remain false throughout.
- Live trading: prohibited; simulation-only requirements remain in force.

## Next mandatory reminder

D-004's initial pre-DXtrade checkpoint is satisfied, and Chapters 1–25 are complete. Chapter 26 Steps 26.1–26.6 are complete, committed, and pushed to `chapter-26-research` (commits through `3e10dc8`), plus a D-019 commit (`ccc415bf`) adopting a research-only box-fade regime band. Step 26.6's real-data run found all four D-009 slots net negative; per D-020 (approved 2026-08-16), Slots 1/2/3 are dropped from further consideration, Slot 4 stays empty/shadow-only per the freeze contract's existing fallback, and the unofficial box-fade experimental strategy becomes the sole strategy under active consideration — conditioned on out-of-sample validation of all 9 leading backtested variants (not just the single leading candidate) before any of it is treated as more than promising, and conditioned on that validation running against DXtrade's own historical market data rather than Binance, per D-020's data-source change. None of D-020's follow-up implementation work has started: `src/dxtradeClient.js` does not yet have a historical-candle method (it authenticates and holds read-only account/instrument/quote-subscription methods only — see `docs/dxtrade-api-endpoint-reference.md` for the exact endpoint, `POST /dxsca-web/marketdata`, that a new method would call); `src/research/strategies/boxFade.js` remains sandbox-only, not committed to the repository; no out-of-sample harness exists yet. Each of these is its own step requiring its own security review and separate owner confirmation before implementation begins — do not build more than one at a time without checking in. Keep Stage A and both execution locks unchanged throughout. Exact DXtrade quantity remains blocked until instrument lot rules are verified. The displayed `$200` Stage A risk cap cannot place a trade; D-009's `$100` restricted-canary cap must be implemented and verified before activation. Any new critical/high security finding or material security regression blocks progression until corrected and rechecked.

Before implementing D-008, complete the current trading automation roadmap and D-007, demonstrate stability, perform a fresh Codex Security review of the then-current code and agent architecture, and obtain a new approval for exact permissions, data retention, staging, rollback, and merge behavior.

---

# Part 3 — Proposed entries D-021 through D-027 (added 2026-08-19, NOT APPROVED)

**Status: PROPOSED. None of these govern until the owner approves them.** They are recorded here so the direction is visible to developers rather than living only in a side document.

Two earlier drafts numbered D-020 and D-025 were **withdrawn** because committed D-020 already decided their substance — the DXtrade data-source shift, and freezing Slot 4 empty. Committing them would have put two entries in the log making one decision. The remaining five were renumbered, and two genuinely new entries were added: **D-026** (D-002 is otherwise left as an unsatisfiable gate) and **D-027** (Chapter 26's remaining steps are otherwise left undefined).

| ID | Chapter / trigger | Baseline | Decision | Effect / rationale | Affected work | Verification | Status |
|---|---|---|---|---|---|---|---|
| D-021 | Working-method review, 2026-08-17 — relaxes Project Instructions §8 | Every chapter, step and checkpoint stops and waits for explicit owner confirmation before the next begins. | Stop only at steps that (a) change repository state, (b) change safety posture, (c) spend money, or (d) open a holdout or otherwise consume a one-shot research resource. Pure research, analysis and reporting steps proceed without an intervening confirmation. | Removes the largest source of round-trip latency without removing any gate that protects the account. Every safety-relevant stop is retained by construction, because the criteria are defined by safety impact rather than by step count. | All future chapter execution. | Each proceeding step still reports what it did and found; any step meeting criteria (a)–(d) still stops. | **PROPOSED** 2026-08-17 |
| D-022 | Working-method review, 2026-08-17 — relaxes freeze contract §11.1 | The Step 26.1 freeze contract lists the exact files Chapter 26 may create; adding any file outside that list requires its own decision-log entry (this produced D-017 and D-018, both substantively "we added a file"). | New files created **under `src/research/` and `scripts/` only** no longer require their own decision-log entry. Any change to a production file — `index.mjs`, `config/*.json`, `src/config.js`, `src/database.js`, `src/telegramBot.js`, `src/tradeifyService.js`, `src/dxtradeClient.js`, `src/signalEngine.js`, `src/signalAlerts.js`, `src/indicators.js`, `package.json` — still requires one. | Removes ceremony with negligible safety value. The isolation property that matters is unchanged: research files are still never imported by `index.mjs`, so a new research file cannot reach the live worker whether or not an entry exists. | All future research work. | Freeze contract §13 check 4 (`index.mjs` and both config files unchanged) still enforced by `git diff` at every step. | **PROPOSED** 2026-08-17 |
| D-023 | Working-method review, 2026-08-17 — relaxes D-016 | D-016 requires all Chapter 26 research evidence to be written to gitignored `artifacts/` files and forbids any PostgreSQL schema change, to protect `src/database.js`, which the live worker depends on at startup. | Research evidence may use PostgreSQL, by either (a) an additive migration to the existing schema with tests, or (b) a separate research database with its own connection string. Neither may alter or drop an existing table, column or constraint used by the live worker. | Multi-asset research at 5-minute granularity across 15–20 symbols is ~2.1M rows. The protective intent of D-016 — never destabilise the live worker's startup path — is preserved by the additive-only and separate-database constraints. **Note:** the current build plan recommends staying on flat files until the universe grows materially; this entry permits PostgreSQL, it does not require it. | Any future research storage layer; `src/database.js` if option (a) is used. | Live worker starts successfully after any migration; `/health` still reports PostgreSQL OK (owner-reported); existing `bars`/`bot_state`/`daily_ledger`/`events` behaviour covered by unchanged tests. | **PROPOSED** 2026-08-17 |
| D-024 | Working-method review, 2026-08-17 — relaxes D-013 | D-013 requires a research burn-in of 150×15m / 40×4h / **40×1d** completed bars, stricter than production's 50/40/25, because Wilder ADX(14) is unstable at ~1.8× its period. | Research burn-in may use production's **25** completed daily bars where test-window length is the binding constraint, provided any result derived from days between bar 25 and bar 40 of an asset's history is flagged as computed on an unconverged ADX reading. | Recovers ~15 days of usable test window per asset, which matters if DXtrade's 5-minute retention proves shallow. The original concern is not dismissed — it is downgraded from a hard exclusion to a disclosure requirement. | Regime classification burn-in in future research. | Any affected result carries the unconverged-ADX flag in its evidence export. | **PROPOSED** 2026-08-17; apply only where window length binds |
| D-025 | New chapter opened, 2026-08-17 | The Step 26.1 freeze contract governs single-asset (BTC), Binance-sourced, four-slot research. It was drafted assuming at least one slot would show an edge and has no defined path when none does. It is the wrong shape for multi-asset DXtrade work. | Open a **new chapter** for multi-asset DXtrade research with its own purpose-built contract, rather than extending or overriding the Step 26.1 contract. Scope: DXtrade as sole data source; percentile-normalised regime definition; data-driven instrument universe selection; per-(asset, direction) qualification; box-fade as a governed candidate. The Step 26.1 contract remains historically authoritative for Chapter 26 only. Chapter number assigned when the contract is written. | Separates a finished, honestly-negative piece of work from a new one, so neither distorts the other. Avoids progressively overriding a frozen contract until it no longer means anything. | New chapter contract document; all subsequent research. | Contract approved by owner before any implementation; drafted only after probe results are known, since test-window design depends on DXtrade's actual 5-minute retention. | **PROPOSED** 2026-08-17; contract not yet drafted |
| D-026 | Data-source review, 2026-08-18 — narrows D-002 | D-002 requires ≥24 continuous hours of **Binance-versus-DXtrade** feed comparison before restricted canary operation, observation of ≥1 Tradeify account-day reset, defined pass/fail thresholds, and ≥7 days of continued comparison during canary. D-020 retires Binance as the research source but explicitly states it "does not change … D-002's unimplemented comparison gate." | Retire **only** D-002's Binance-versus-DXtrade cross-feed comparison requirement, as moot: under D-020 research and execution read the same DXtrade feed, so there is no second feed whose basis must be validated. **All other D-002 requirements survive unchanged and remain blocking**: observation of at least one Tradeify account-day reset, defined measurable pass/fail thresholds, ≥7 days of monitoring during restricted canary operation, and no risk increase until extended monitoring and live execution results are reviewed. Those are re-scoped from cross-feed comparison to **DXtrade-internal consistency**: REST candle history versus live streaming quotes versus actual fill prices. | Without this entry D-002 is an orphaned gate — a blocking live-execution requirement satisfiable only by re-introducing the Binance fetch path D-020 just retired, purely as ceremony. But retiring D-002 *wholesale* would delete real safety content: the account-day-reset observation and the 7-day canary monitoring concern live-platform behaviour, not Binance, and are not moot. This removes the dead half and preserves the live half. | Validation gates, monitoring, readiness reporting, canary-risk review; D-025's new chapter contract must define the DXtrade-internal thresholds. | Pass/fail thresholds defined and recorded before canary; ≥1 account-day reset observed; ≥7 days of DXtrade-internal consistency monitoring during canary; REST-versus-stream-versus-fill differences measured and documented; no risk increase until reviewed. | **PROPOSED** 2026-08-18 |
| D-027 | Chapter 26 terminal status, 2026-08-18 — invokes D-020 | Chapter 26 is parked between Steps 26.6 and 26.7. `docs/chapter-26-slot4-freeze-record.json` does not exist. The Step 26.1 contract's §14 step sequence still lists Steps 26.7 (folds 5–6, validation, qualification), 26.8 (holdout opened once, reports, security review, PR) and 26.9 (decision-log record, close). D-020 dropped Slots 1–3 and left Slot 4 empty/shadow-only but did not state what happens to the remaining step sequence. | Chapter 26 is **superseded, not completed**. Steps 26.7, 26.8 and 26.9 are **cancelled and will not be run**. `docs/chapter-26-slot4-freeze-record.json` is **not written**, because a freeze record's only function is to fix a selected Slot 4 variant before out-of-sample folds run, and D-020 removed both the selection and the reason to run those folds. **The Chapter 26 holdout partition (15m bars 29,201–35,040) is never opened** and remains sealed. The Step 26.1 contract remains historically authoritative for interpreting Steps 26.1–26.6 only. Chapter 26's result of record is the Step 26.6 finding: all four D-009 slots net negative on real data. | Leaves no undefined state. Without this, a future session reads §14, sees three unexecuted steps, and either runs them against a strategy set that no longer exists or re-litigates whether they should. Cancelling is correct rather than merely convenient: running folds 5–6 and the holdout would consume the project's only sealed out-of-sample partition to validate strategies that have already been dropped — spending a one-shot resource on a question nobody is asking. | Steps 26.7–26.9; `docs/chapter-26-slot4-freeze-record.json` (not created); `docs/chapter-26-research-summary.md`; the `chapter-26-research` → `main` PR. | Freeze record confirmed absent; holdout partition confirmed unopened; research summary records the negative result and the supersession; both execution locks remain false. | **PROPOSED** 2026-08-18 |

## 2026-08-17/18 — Multi-asset direction review, constraint relaxation, and Chapter 26 supersession

The owner raised two changes of direction: incorporating additional DXtrade instruments beyond BTC, and reducing process friction that was slowing the build.

**Data source.** Recorded separately as committed D-020. Binance is retired as the historical research source in favour of DXtrade's own REST candle endpoint. This is a strengthening, not a loosening: it eliminates the basis-risk objection that a Binance backtest could never be treated as executable, and it eliminates two measured data-quality problems (the `api.binance.com` geo-restriction, and 31.1% zero-volume 5m bars on `api.binance.us` for SOL). It also means every existing research number must be re-established on DXtrade data before it can be cited — including the +$477.01 box-fade figure.

**Constraint review.** The owner asked for a list of project constraints hindering progress, with a view to overriding them. On review, the constraints causing friction and the constraints protecting the account proved to be largely disjoint sets. Four items are relaxed here (D-021 through D-024); the account-protection set is left entirely intact.

One item the owner specifically raised — freeze contract §12's prohibition on research code making network calls — was **not** relaxed, because it does not in fact block the work. §12 constrains research *modules*; it explicitly contemplates `scripts/run-backtest.mjs` as "a research tool that happens to live in the repository, not a component of the bot," and D-018 already carved out `scripts/export-bars-for-research.mjs` reading the owner's own `DATABASE_URL`. A DXtrade fetch script in `scripts/` fits the existing pattern without amendment.

**D-002.** Retiring the Binance source created an orphan: D-002's cross-feed comparison gate could no longer be satisfied without re-introducing the very fetch path D-020 retired. An earlier draft proposed retiring D-002 outright. On review that would have discarded genuine safety content — the account-day-reset observation and the seven-day canary monitoring have nothing to do with Binance and are not moot. D-026 therefore retires only the cross-feed half and re-scopes the rest to DXtrade-internal consistency.

**Chapter 26.** Earlier drafting proposed closing Chapter 26 by freezing Slot 4 empty and running Steps 26.7–26.9 to a formal completion. Committed D-020 overtook that: with Slots 1–3 dropped and Slot 4 empty, there is nothing left for those steps to evaluate. D-027 records Chapter 26 as **superseded rather than completed**, cancels Steps 26.7–26.9, and leaves the sealed holdout partition unopened. The project's stated philosophy that "a strategy that fails is a result, not a gap" is satisfied by the Step 26.6 finding itself; it does not require a ceremonial completion that would burn a one-shot out-of-sample resource on abandoned strategies.

**Deliberately NOT changed.** Recorded so a future session does not re-litigate settled ground:

- Railway `AUTO_EXECUTE=false` and `config/strategy.json` → `execution.autoExecute=false`. Both locks remain false.
- No order placement, order routing or bracket capability before lot rules, commissions, stop/OCO behaviour, idempotency, rejection handling and reconciliation are verified against the live platform.
- Risk floors, daily loss controls, MLL tracking, durable pause, restricted resume and account lockout.
- One open position maximum. Considered and kept: all 75 blocked signals fired in the *same* direction as the open trade, so relaxing this buys concentration, not diversification.
- Hard-flat at 21:45 UTC. Considered and kept. It is what makes D-015 (overnight cost not modelled) sound, and the $1,500 daily limit is computed on live equity **including unrealised losses**, so an unattended overnight position can breach it with no order path built to defend against that.
- Never requesting or exposing Telegram tokens, owner IDs, database credentials, DXtrade credentials, API keys or session tokens in chat, code, logs, screenshots or documentation.
- Exactly one Telegram polling process. No research module imported by `index.mjs`.
- No strategy-specific research finding written into `config/strategy.json` without first confirming which strategy that file governs (the near-miss corrected in D-019).
- The Chapter 26 holdout partition stays sealed (D-027). The new chapter will define its own freeze and holdout discipline; it will not inherit none.

**Next.** A read-only DXtrade probe determines auth, the account's entitled instrument list, and candle retention depth at `d` and `5m` separately. The new chapter contract (D-025) is drafted only after those numbers are known, because the achievable test window depends on DXtrade's actual 5-minute retention — which is unknown and, on most broker platforms, materially shallower than daily retention.
